#pragma once

//==========================================================
// AssetPack
//   Distribute ビルド専用：exe に埋め込んだ assets.pak を
//   exe と同じフォルダへ自己展開し、カレントディレクトリを
//   そこへ移す。これにより既存の "Asset/..." 相対パス読み込みが
//   そのまま動く（単一 exe を配布すれば遊べる）。
//
//   Debug / Release ビルドでは Prepare() は何もしない（開発時は
//   プロジェクトフォルダ直下の Asset/ をそのまま使う）。
//==========================================================
namespace AssetPack
{
    // 配布ビルドなら埋め込み pak を展開し、CWD を exe フォルダへ移す。
    // 通常ビルドでは何もしない。アプリ起動時の最初に一度だけ呼ぶこと。
    void Prepare();
}
